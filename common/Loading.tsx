import React from 'react';
import { Loader2 } from 'lucide-react';

const Loading = () => {
  return (
    <Loader2 size={50} color='#635FC7' className='w-1/2 mx-auto animate-spin' />
  );
};

export default Loading;
